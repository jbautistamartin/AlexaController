<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Permite HTTP para redes locales (ngrok siempre usa HTTPS) -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
